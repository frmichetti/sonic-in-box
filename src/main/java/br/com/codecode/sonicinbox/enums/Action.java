package br.com.codecode.sonicinbox.enums;

// TODO: Auto-generated Javadoc
/**
 * Possible Actions.
 *
 * @author felipe
 * @version 1.0
 * @since 1.0
 */
public enum Action {

    /** The stop. */
    STOP, /** The wait. */
 WAIT, /** The move. */
 MOVE, /** The walk. */
 WALK, /** The run. */
 RUN, /** The down. */
 DOWN, /** The downed. */
 DOWNED, /** The dash. */
 DASH, /** The jump. */
 JUMP, /** The spin. */
 SPIN, /** The look. */
 LOOK, /** The looking. */
 LOOKING, /** The push. */
 PUSH, /** The transform. */
 TRANSFORM, /** The speedup. */
 SPEEDUP, /** The brakeup. */
 BRAKEUP;

}
